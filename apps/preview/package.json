{
  "name": "@programmable-video/preview",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "types:check": "wrangler types worker-configuration.d.ts --check",
    "types:worker": "wrangler types worker-configuration.d.ts"
  },
  "dependencies": {
    "@programmable-video/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.125.0"
  }
}
